# Optional: copy to `.env` (gitignored). Run challenge server on :3000, then `npm run dev`.
# See src/config.ts for defaults.

VITE_API_BASE_URL=http://localhost:3000/api/v1
VITE_TENANT_ID=123
VITE_ACTION_BLUEPRINT_ID=bp_456
